# Working Agreements

- Always run `./gradlew test` after changes that affect behavior.
- Do not add new dependencies without justification in `docs/design-decisions.md`.
- Prefer small PR-sized commits; include clear commit messages.
